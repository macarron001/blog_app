%br/
%br/
%table.table
  %thead
    %tr
      %th Title
      %th Content
  %tbody
    - @articles.each do |article|
      %tr
        %td
          = article.title
        %td
          = article.body
        %td
          = link_to 'Show', article_path(article.id)
= link_to 'New Article', new_article_path
